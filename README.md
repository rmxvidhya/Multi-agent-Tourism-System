# Tourism Agent Backend

Backend server for multi-agent tourism planning system.

## Setup
1. Install dependencies: `npm install`
2. Create `.env` file with your ANTHROPIC_API_KEY
3. Run: `npm start`

## Endpoints
- POST /api/plan-trip - Plan a trip
- GET /health - Health check